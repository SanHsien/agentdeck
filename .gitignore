.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
.claude/settings.local.json
*.log
pricing_cache.json
# Runtime cache the app writes when run from source (cache/projects.json)
/cache/
agentdeck.log
agentdeck.err.log
.DS_Store
build/
dist/
*.egg-info/
# Windows PyInstaller output (scripts/build_windows.ps1) and its release archive.
# Vendors a full copy of the deps, which otherwise drowns ruff/mypy in errors.
winbuild/
agentdeck-windows.zip
agentdeck-windows.zip.sha256
# Compiled instate-cli binary (built from the separate ~/Developer/instate project).
# Contains private role content — must never enter usage's public git history.
vendor/
.pytest_cache/
.coverage
.antigravitycli/

# Local session handoff notes (per-machine, not source)
SESSION.md

# Stray Instagram / scratch image dumps in repo root
/ig_*.png

# Misplaced Claude auto-memory (real location is ~/.claude/projects/.../memory/)
/memory/

# 依賴新鮮度報告（每次執行重新產生，不追蹤）
dependency-freshness-report.md
upstream-check-report.md
